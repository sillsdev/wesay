#!/bin/bash

set -ueo pipefail
script_dir="$(dirname "$0")"
source "${script_dir}/../lib/wesay/setup-wesay.bash"

export MONO_DEBUG=explicit-null-checks
export MONO_ENABLE_SHM=1
exec mono --debug "${WESAY_PREFIX}/lib/wesay/WeSay.ConfigTool.exe" "$@"
